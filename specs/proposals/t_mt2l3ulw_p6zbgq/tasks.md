# 任务清单：任务面板的UI 帮我优化

> 由任务面板规划阶段生成（OpenSpec 风格）。开发阶段按编号逐步实施，每完成一项将 `[ ]` 改为 `[x]`，复核阶段逐项核对。

- [x] 1. 1. 建立内联 SVG 图标系统（client.js）：新增 ICONS 映射（Feather/Lucide 风格 24×24 stroke path，currentColor）与 Icon({name,size}) 辅助函数（React.createElement 实现，不用 JSX）；覆盖图标清单：plus/send（发布）、search（扫描）、rocket（领取）、play（继续）、pause（暂停）、check（确认/澄清提交）、check-circle（验收通过/复核通过）、x-circle（复核未通过）、rotate-ccw（打回/重新打开）、refresh（重新执行/刷新）、trash（删除）、pencil（编辑）、chevron-down（展开）、x（关闭）、alert-triangle（持久化警告）、clipboard/list（面板标题）、loader（运行中 spinner）、link/clock/folder（元信息 chips，可选）。验收：node --check client.js 通过；面板打开后侧栏/头部/卡片按钮图标全部渲染、无裂图。
- [x] 2. 2. 发布区改为按钮 + 弹窗（client.js）：删除 PublishBox 的内联折叠（tp-pub-toggle/▸▾），在 body 顶部渲染「发布新任务」按钮（icon+文字）；新建 PublishDialog 弹窗（mask + 居中卡片），表单复用原字段（标题/描述/验收标准/仓库下拉/自动执行/自动确认/扫描关联会话+候选勾选/发布/取消），发布成功自动关窗、清空表单并 refresh()，失败在弹窗内展示错误；store 增加 modal 字段（null|{kind:'publish'}），DrawerBody 的 Esc 处理改为「弹窗优先关闭，其次关面板」。验收：点按钮弹出弹窗；填表发布成功→任务进待领取且弹窗自动关闭；Esc/遮罩/✕ 均可关闭；扫描勾选与同名任务校验行为不变；标题为空时发布按钮禁用或提示。
- [x] 3. 3. 全部状态操作按钮图标化（client.js TaskCard）：将 actions 的 emoji 标签（🚀 领取并规划 / 🗑 删除 / ✅ 提交澄清并定稿 / ✅ 确认计划 / ↩️ 打回 / 🔄 重新执行 / ✅ 验收通过 / ↩️ 打回开发 / ↩️ 重新打开）改为 Icon+文字组合渲染（tp-btn 内 gap 布局），busy 态显示「处理中…」（可加 spinner）；同时给 todo/clarify/confirm/develop/review 各状态追加「⏸ 暂停」按钮（非主操作样式）。验收：六个 Tab 下每个状态按钮均显示图标+文字且无 emoji；逐项点击后状态流转与改造前完全一致（claim→规划、confirm→开发、accept→done、reject/reopen→回退、rerun→重跑、delete→删除）。
- [x] 4. 4. 装饰性 emoji 全部替换（client.js）：卡片展开箭头 ▴/▾ → SVG chevron-down（按 expanded 旋转 180° 过渡）；运行中「⏳」→ spinner SVG + 文案；复核报告「✅ 通过/❌ 未通过」→ check-circle/x-circle + 文案；头部标题「📋 任务面板」→ clipboard 图标+文字、持久化「⚠️」→ alert-triangle、刷新「🔄」与关闭「✕」头部按钮 → SVG；footer「⚠️ 数据保存失败…」→ alert-triangle + 文案。验收：grep 正则确认 client.js 无 emoji/▸▾ 字符残留；明暗主题下图标颜色随 --tp-* 变量变化；宽度拖拽、展开/收起动画正常。
- [x] 5. 5. 服务端新增 paused 状态与 pause/resume 动作（index.js）：STATUS 增加 PAUSED='paused'、STATUS_LABEL.paused='暂停中'、STATUS_COLOR.paused（建议 #f97316 橙）、STATUS_ORDER 在 develop 之后插入 paused；loadState 归一化 t.pausedFrom/pausedFromRunning 兜底；runStage 把 makeSignal 的 AbortController 挂到 task.flags.abort 供暂停中断；handlers 增加 pause（非 done/paused 可暂停：记录 pausedFrom 与 pausedFromRunning，若 flags.running 则 abort 并让各 runner 的 finally 正常释放名额，move 到 paused 并留痕）、resume（仅 paused：恢复 pausedFrom，若 pausedFromRunning 则按原状态重踢对应阶段——todo→maybeAdvanceQueue、develop→runDevelop、review→runReview，否则仅恢复并留在原状态；清空 pausedFrom 标记）；扩展 edit（仅 paused 任务可编辑标题/描述/验收，编辑后清空 plan/planDraft/steps/risks/summary/reviewReport/changedFiles 并 note 留痕，使下次运行按新方向重新生成）；toSummary 增加 pausedFrom/pausedFromRunning；countsOf 增加 paused 键；队列守卫确认 todoQueue 不会捞起 paused（现状天然满足）。验收：curl POST /dsh-task-panel/api/tasks-action 分别测 pause/resume/edit：状态迁移正确、pausedFrom 记录、编辑后旧产物被清、paused 任务不被队列自动领取、运行中任务 pause 后子代理中止且名额正确释放。
- [x] 6. 6. 客户端暂停/继续/重新编辑 UI（client.js）：store 默认 order/labels/colors 增补 paused（与 tasks-list 下发一致）；paused Tab 与计数自动出现；paused 任务卡片动作 = 继续执行(resume，primary)/重新编辑(edit)/删除(delete)；新建 EditDialog 弹窗（复用 store.modal，{kind:'edit',taskId}，表单预填标题/描述/验收标准，保存→tasks-action edit→refresh 关窗）；卡片展开区显示暂停来源（如「已暂停（来自 开发中）」）；侧栏角标计入 paused（todo+confirm+review+paused，README 同步口径）。验收：任意非 done 任务点「暂停」→ 进暂停中 Tab 且不再被领取/执行；「继续执行」恢复到暂停前状态（开发中任务会重新开跑）；「重新编辑」改字段保存后任务方向生效（后续运行使用新标题/描述/验收）；删除仍可用；角标数字随 paused 变化。
- [x] 7. 7. 样式与主题、弹窗细节（client.js CSS）：新增 .tp-dialog-mask/.tp-dialog/.tp-dialog-header/.tp-dialog-body/.tp-dialog-actions 等样式（z-index 高于抽屉 1000，圆角 12px、阴影、明暗主题复用 --tp-bg/--tp-fg/--tp-accent 变量）；按钮 icon+文字统一样式（.tp-btn 内 flex gap 6px）；spinner 关键帧动画、chevron 旋转过渡、focus-visible 焦点环、按钮 title 提示。验收：prefers-color-scheme 切换后弹窗/按钮/图标颜色均正常；拖拽宽度、Esc/遮罩关闭、键盘 Tab 聚焦可见性正常。
- [x] 8. 8. README 与文档同步（README.md）：§1 状态表增加「暂停中 paused」行；§2 流程图加入 暂停→编辑→继续 分支；§4 面板 UI 描述改为「发布新任务按钮 + 弹窗」并说明图标化与编辑功能；清除文档中 📋🔍📖✅ 等 emoji 引用（Q3 要求全部替换）；§3/§6 如有涉及状态流转的表述同步。验收：grep 确认 README 与实现一致、无按钮/图标相关 emoji 残留。
- [x] 9. 9. 端到端验收（重启 dsh web + GUI 逐项验证）：重启 dsh web（managed background job，勿起替代服务器），页面刷新 http://127.0.0.1:3080 后按老板确认的验收清单逐项过：① 所有按钮无 emoji/▸▾，统一图标+文字；② 点「发布新任务」弹窗，发布成功任务进待领取且自动关窗，Esc/遮罩/✕ 可关闭；③ 领取/澄清/确认/打回/验收/重新执行/删除图标替换后功能不变；④ 明暗主题与宽度拖拽正常；⑤ 零外部网络依赖（grep client.js 无 http(s) 资源引用、无外部字体加载）；⑥ 新增：装饰性 emoji 全部替换、每个任务可暂停、暂停后可重新编辑调整方向、继续执行恢复流转；用 screenshot 技能截图留证（明暗两主题 + 弹窗 + 暂停/编辑闭环）。验收：老板按上述清单确认全部通过。
